-- Выполняется один раз при инициализации контейнера с PostgreSQL.
-- Тесты (соединение pgsql_test) пересоздают схему на каждом прогоне,
-- поэтому им нужна отдельная база, а не та, с которой работает приложение.
CREATE DATABASE hexlet_sicp;
CREATE DATABASE hexlet_sicp_test;
