Сколько сложений происходит при вычислении `n` -го числа Фибоначчи, в случае, когда мы используем определение `fibs` через процедуру `add-streams` ? Покажите, что число сложений выросло бы экспоненциально, если бы мы реализовали `(delay &lt;exp&gt;)` просто как `(lambda () &lt;exp&gt;)` , без оптимизации через процедуру `memo-proc` из раздела 3.5.1.
