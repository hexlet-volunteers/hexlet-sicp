Modify the compiler to maintain the compile-time environment as described above. That is, add a compile-time-environment argument to `compile` the various code generators, and extend it in `compile-lambda-body` .
